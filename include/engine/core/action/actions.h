#pragma once
#include <variant>

namespace cursed_engine
{
	// TODO; look into Commands again (Command Pattern)

	//class ActionRegistry;
// Make struct???
	//class Action
	//{
	//public:
	//	using Args = std::variant<bool, int, float, std::string, Entity>;

	//	//Action(std::span<Args> args);
	//};

	namespace core_actions
	{
		//void registerAll(ActionRegistry& actionRegistry, EventBus& eventBus);
	}
}